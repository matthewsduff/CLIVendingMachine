package com.techelevator;

import java.util.Scanner;
import java.util.List;


public class MoneyHandler {
 
	//int moneyInput =  * 100;
	
	// recieve user money input
	// Scanner
	
	// convert whole dollars into cents i*100
	// store into variable currentMoneyProvided
	
	// purchase menu -- brings customer back to this step after selecting and dispensing an item
	
	// customer selects item, check if they have enough balance and item is available/not sold out
	// if both are TRUE -- subtract from currentMoneyProvided
	
	// item gets dispensed to customer -- store the item in a variable
	// reduce the quantity of the item selected by 1
	
	// returns customer to the purchase menu 
	
	// after purchase transaction convert back into whole dollar amounts iChange / 100 
	// returning quarters, dimes, nickels
	// store change into variable customerChange
}
